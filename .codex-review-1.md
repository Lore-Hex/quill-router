=== codex verdict on quill-router PR #1159 @ 27d2319f ===
Found nine release-blocking defects.
### (a) Migration
- **P1 — [scripts/deploy/migrate_receipt_key_versions.sh:39]:** An index left `CREATING` by an interrupted operation makes every subsequent deploy poll for 30 minutes and fail, although the runtime never uses that index. Reproduce by leaving `tr_receipt_key_versions` non-`READ_WRITE` and running the workflow. Fix: ga
- **P1 — [storage_postgres_schema.sql:65], [storage.py:3870]:** Every Postgres process applies a regular, non-partial `CREATE INDEX` at startup; on stock Postgres this indexes all shared-table rows, including millions of all-NULL entries, and blocks writes during the initial build. Reproduce by starting a new revisio
- No additional migration defect found: the Spanner columns are correctly nullable, the metadata queries match the repository’s established GoogleSQL pattern, and the null-filtered index is not rebuilt on normal deploys. Repository evidence says `tr-deploy` was granted `roles/spanner.databaseAdmin`, which includes `s
### (b) Stores
- **P1 — [storage_gcp.py:7705], [storage_postgres.py:801]:** Both durable stores fetch and deserialize every receipt row before applying `kid` or `limit`; the new index is wholly unused. Reproduce with a large version log and request one nonexistent `kid`: the complete log is still read. Fix: implement parameterized 
- **P1 — [receipt_key_collector.py:237], [storage_gcp.py:7721]:** Current and historical documents receive the same observation time, and every collection refreshes every history row; once the log exceeds 5,000, hash/kid tie-breaking can exclude a live instance’s current document. Reproduce with 5,001 refreshed ver
- Kid collision validation is cryptographically bound by recomputing `kid` from the JWK; the legacy rewrite itself is transactional in Spanner and row-locked in Postgres. I found no separate legacy-race defect.
### (c) Collector
- **P1 — [receipt_key_collector.py:144]:** Historical GCP documents are verified against wall-clock expiry, so a correctly signed expired document is skipped and cannot repair a collector outage—the principal purpose of `att_history`. Reproduce with valid history whose `exp` is more than five minutes old. Fix: for 
- **P1 — [receipt_key_collector.py:189]:** History count is unbounded; a sub-2 MB array containing hundreds of thousands of invalid scalar entries causes an equally large validation/logging loop, while the response-size check occurs only after HTTPX buffers the entire response. Fix: stream with a hard byte cutoff, ca
- **P1 — [receipt_key_collector.py:190]:** The broad history exception handler swallows storage failures as malformed-entry skips; the route then records a healthy collector heartbeat. Reproduce with a store that accepts current but raises on history: `errors == 0` and the job succeeds without recording history. Fix:
- Current-JWK binding and claimed `att_sha256` handling are correct: each entry recomputes `kid`, checks its commitment against the current JWK, and compares the claimed hash to decoded bytes.
### (d) Public route
- **P1 — [public.py:691], [public.py:2075]:** Arbitrary, unvalidated `kid` values become permanent dictionary keys; unique queries provide an unauthenticated memory-growth attack and each also triggers the full-store scan above. Reproduce with many unique `?kid=x-N` requests. Fix: require the canonical 43-character b
- **P1 — [public.py:727], [public.py:2107]:** `s-maxage=3600` exceeds the 30-minute re-mint period, so a compact receipt’s newly observed document may remain absent from shared-cache responses for an hour. Fix: use a TTL shorter than collection/re-mint cadence with ETag revalidation, or `no-cache`.
- **P1 — [public.py:2080]:** The route materializes up to 5,000 complete attestations into one JSON response and in-process cache; version growth turns the global endpoint into a potentially tens-of-megabytes response. Fix: add cursor pagination and a response-byte ceiling, while making `?kid=` the compact-receipt lo
- There is no direct SQL injection: `kid` currently never reaches SQL. The resulting application-side scan and cache-key injection are the actual vulnerabilities.
### (e) Other changed material
- **P2 — [docs/client-receipts.md:47]:** The documentation says the migration is safe after the compatible router, but the new writer immediately names physical `kid` and `att_sha256` columns and fails if they do not exist. Reproduce by starting this revision before applying DDL and collecting a key. Fix: state expli
- **P2 — [blog.py:2176]:** The blog says every re-mint is “verified before it is recorded,” while AWS and Azure are only commitment-checked and published as `verified=false`. Fix: say “commitment-checked before recording and fully chain-verified where supported.”
- `deploy.yml` otherwise places the additive Spanner columns before rollout as required.
VERDICT: reject
[exited with code 0]
