=== codex verdict on router PR #1159 @ 235eea7e ===
2080:            headers=public_document_headers("/trust/control-plane.json"),
codex
### Findings
- **P1 — The Spanner migration does not gate on a stuck index.** [migrate_receipt_key_versions.sh] checks only whether the index exists; it never reads `INDEX_STATE`. With a fake existing `CREATING` index, the script made three `COUNT(*)` queries, print
- **P1 — The Postgres partial index is not created at all, and indexed reads are not guaranteed on either durable backend.** [storage_postgres_schema.sql] adds the columns but contains no `tr_receipt_key_versions` index, and no separate Postgres migrati
- **P1 — Historical observations still refresh durable ordering timestamps and can evict current keys.** `refresh_last_seen=False` preserves the JSON field, but `merge_receipt_key_observation` still returns `"refreshed"`. Both stores consequently upsert the row ([Postgres](/private/tmp/claude-501/-Users-jperla-josh/2b18523f-4577-4745-8569-616d3cfbbe52/scratchpad/qr/src/trusted_router/storage_pos
- **P1 — The public listing remains large enough for response and cache exhaustion.** [public.py] still fetches and serializes up to 5,000 attestations without a byte ceiling or pagination. Reproducing with 5,000 distinct keys and 8 KiB attestations produced a *
- **P1 — History storage errors still produce a healthy collector heartbeat.** The collector now correctly returns `errors=1`, but [gateway.py] unconditionally records the heartbeat and returns HTTP 200. Reproduction with `errors=1` yielded `status=200
- **P2 — Public documentation now contradicts the route.** [receipts.html] says the unfiltered endpoint serves every observed re-mint, while the route deliberately serves only one version per `kid`. [client-receipts.md](/private/tmp/claude-501/-User
The expired-history cryptographic checks, streaming byte cutoff, 64-entry loop bound, canonical `kid` validation, unknown-kid cache exclusion, 300-second shared TTL, ETag handling, and the originally requested blog/migration wording corrections do work. Full Ruff and mypy pass; targeted receipt/conformance/blog tests pass 35 with 3 backend-dependent skips.
VERDICT: reject
tokens used
175,389
### Findings
- **P1 — The Spanner migration does not gate on a stuck index.** [migrate_receipt_key_versions.sh] checks only whether the index exists; it never reads `INDEX_STATE`. With a fake existing `CREATING` index, the script made three `COUNT(*)` queries, print
- **P1 — The Postgres partial index is not created at all, and indexed reads are not guaranteed on either durable backend.** [storage_postgres_schema.sql] adds the columns but contains no `tr_receipt_key_versions` index, and no separate Postgres migrati
- **P1 — Historical observations still refresh durable ordering timestamps and can evict current keys.** `refresh_last_seen=False` preserves the JSON field, but `merge_receipt_key_observation` still returns `"refreshed"`. Both stores consequently upsert the row ([Postgres](/private/tmp/claude-501/-Users-jperla-josh/2b18523f-4577-4745-8569-616d3cfbbe52/scratchpad/qr/src/trusted_router/storage_pos
- **P1 — The public listing remains large enough for response and cache exhaustion.** [public.py] still fetches and serializes up to 5,000 attestations without a byte ceiling or pagination. Reproducing with 5,000 distinct keys and 8 KiB attestations produced a *
- **P1 — History storage errors still produce a healthy collector heartbeat.** The collector now correctly returns `errors=1`, but [gateway.py] unconditionally records the heartbeat and returns HTTP 200. Reproduction with `errors=1` yielded `status=200
- **P2 — Public documentation now contradicts the route.** [receipts.html] says the unfiltered endpoint serves every observed re-mint, while the route deliberately serves only one version per `kid`. [client-receipts.md](/private/tmp/claude-501/-User
The expired-history cryptographic checks, streaming byte cutoff, 64-entry loop bound, canonical `kid` validation, unknown-kid cache exclusion, 300-second shared TTL, ETag handling, and the originally requested blog/migration wording corrections do work. Full Ruff and mypy pass; targeted receipt/conformance/blog tests pass 35 with 3 backend-dependent skips.
VERDICT: reject
[exited with code 0]
